@AGENTS.md

# Leila Photography

## Project

- This is the Next.js frontend for Leila's photography website.
- The CMS will be Strapi; keep the frontend and CMS concerns separated.
- Use the App Router and TypeScript conventions already established by this project.

## Development

- Use the existing npm scripts in `package.json` for development, linting, and builds.
- Run `npm run lint` and `npm run build` after meaningful changes.
- Read the relevant Next.js documentation in `node_modules/next/dist/docs/` before changing framework behavior or APIs.

## Changes

- Preserve the generated Next.js guidance in `AGENTS.md`.
- Prefer small, focused changes that match the existing project structure.
- Do not add CMS integration, dependencies, or abstractions until the required content model and API contract are known.
